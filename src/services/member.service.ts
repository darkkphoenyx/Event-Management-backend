import { PrismaClient } from '@prisma/client'
const Prisma = new PrismaClient()

//submit form 
export const submitForm =async () => {
    //post data to DB
    // this will be further verified by the Admin and the only executed
}